import { Router } from 'express';

// Thin module — the email campaign route is handled within the admin module.
// This file exists for structural completeness.
const router = Router();

export default router;
